<x-navbar>
    {{-- UPPER PART --}}
    <x-header.upper-part name="{{ $auth->name ?? '???' }}" header="Job Order" />

    {{-- Main Content --}}
    <hr>
    <div class="m-10 px-6">
        try
    </div>
</x-navbar>
